pipeline {
    agent any  // Usar qualquer agente disponível

    stages {
        stage('Hello World') {
            steps {
                script {
                    echo 'Hello, World!'  // Imprime a mensagem no console
                }
            }
        }
    }
}
